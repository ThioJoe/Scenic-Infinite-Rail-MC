# Time mode:  /function infinite_rail:mode_day_off
# Alias for mode_night_off -- both frozen time options (night only / day
# only) end the same way: back to the default day/night cycle.
function infinite_rail:mode_night_off
